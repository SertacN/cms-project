model Category{
    id Int @default(autoincrement()) @id
    title String @unique
    sefUrl String @unique
    orderBy Int @default(0)
    isActive Boolean @default(true)
    content Content[]
    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt
    deletedAt DateTime?
    isDeleted Boolean @default(false)
}